const Spinner: React.FC = () => {
  return (
    <div className="spinner-circle [--spinner-color:var(--blue-6)]">
      <p className="sr-only">Loading...</p>
    </div>
  );
};

export default Spinner;
